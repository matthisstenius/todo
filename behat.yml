default:
    autoload:
        '': %paths.base%/tests/behat/features/bootstrap
    suites:
        domain:
            paths: [ %paths.base%/tests/behat/features]
            contexts:
              - FeatureContext
              - TodoContext

